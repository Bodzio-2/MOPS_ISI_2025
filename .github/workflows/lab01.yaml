name: lab01
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: pip install nbconvert
      - run: sudo apt-get install pandoc inkscape
      - run: python -m nbconvert --to pdf lab1/MPS_lab1.ipynb