name: lab01
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: pip install nbconvert
      - run: sudo apt-get install pandoc
      - run: python -m nbconvert --to pdf MPS_lab1.ipynb